<!DOCTYPE html>

%html
  %head
    %title Tys
  %body
    %table
      %tr
        %td = @report.type
        %td = @report.stack
        %td
          %ul
            = @devices.each do |device|
              %li device.name
        %td = @most_recent.to_s
        %td = @first_time.to_s
